<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SaveMore</title>
    <!-- <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" /> -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">

    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="font-['helvetica_now_display']">
    
    <nav class="w-full flex items-center justify-between bg-gray-800 text-white px-5 py-3">
        <h3 class="text-xl font-bold">SaveMore</h3>
    
        <% var loggedin = typeof loggedin !== "undefined" ? loggedin : true %>
        <% if (loggedin) { %>
            <div class="flex items-center gap-5">
                <a href="" class="hover:text-gray-400 transition">Shop</a>
                <a href="/cart" class="hover:text-gray-400 transition">Cart</a>
                <a href="" class="hover:text-gray-400 transition">My Account</a>
                <a href="/users/logout" class="text-red-600 hover:text-red-400 transition">Logout</a>
            </div>
        <% } %>
    </nav>
    